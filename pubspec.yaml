name: nocterm_components
description: A component library for Nocterm used by Serverpod.
version: 0.0.1

environment:
  sdk: ^3.11.0

# Add regular dependencies here.
dependencies:
  intl: ^0.20.2
  nocterm: ^0.6.0
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
